# 《看懂人工智能》概念长文写作与排版规范（SPEC）

本文档定义《看懂人工智能》全书（5 大主线专题 + 1 组基础知识 · 共 42 篇）概念长文的统一写作风格、大纲骨架、分层认知模型、排版视觉规范与质量交付流程。所有文章的撰写、配图与代码卡片生成均须严格遵循此标准。

---

## 一、 核心叙事哲学与分层认知漏斗（Philosophy & Cognition Funnel）

本书的定位是 **「硬核但不晦涩、严谨但极其好懂的 AI 通识与心智模型构建指南」**。
读者群涵盖：转型 AI 的软件工程师、AI 产品经理、技术运营、大学生及深度科技爱好者。他们需要搞懂**底层物理与工程因果**，而不是被晦涩的学术论文公式或复杂的张量算子劝退。

因此，全书正文写作必须严格贯彻 **「分层认知漏斗（Layered Cognition Funnel）」**：

```
┌─────────────────────────────────────────────────────────────┐
│  第 1 层：具象生活隐喻 / 场景切入（5秒建立直觉，0门槛读懂）    │
├─────────────────────────────────────────────────────────────┤
│  第 2 层：物理因果与工程运转机制（大白话拆解，不堆砌生僻数学符号） │
├─────────────────────────────────────────────────────────────┤
│  第 3 层：极简透明代码 / 实验印证（15~30行极简逻辑，带清晰输入输出）│
├─────────────────────────────────────────────────────────────┤
│  第 4 层：工业账本与现实权衡（显存、算力、成本与延迟的真实取舍） │
├─────────────────────────────────────────────────────────────┤
│  第 5 层：击碎误区与清晰认知（读到这里该能分清 + 承上启下）     │
└─────────────────────────────────────────────────────────────┘
```

### 核心写作准则：
1. **生活直觉先行**：
   - 每一个新概念出场，必须先给出一个恰当的生活常识比喻（如：MoE 是智库顾问按需叫进会议室、Prompt Caching 是大厨预备葱蒜底料、KV Cache 是收银员手边的便签纸）。
2. **物理因果导向，拒绝玄学拟人**：
   - 严禁将大模型描述为「有意识、有自我欲望、有情绪」的生物形态。
   - 必须将其还原为**浮点数权重矩阵、自回归统计概率预测、损失函数梯度更新与 GPU 显存/带宽吞吐**的工程物理系统。
3. **拒绝公式劝退与学术黑话**：
   - 严禁直接甩出未加解释的复杂数学公式；
   - 若出现核心公式（如 Softmax 温度缩放、Top-P 累加、MoE 门控），必须在紧接着的一句话中**用大白话解释每个字母代表的物理含义**（例如：“$T$ 就像是一个激进程度的旋钮，把 $T$ 调大，所有词的概率差距就会被强行抹平……”）。
4. **代码定位：极简可运行的认知玩具**：
   - 代码卡片必须控制在 **15~30 行核心逻辑**，杜绝冗长的底层算子与复杂的 Tensor 维度重排；
   - 必须配有模拟输入与控制台输出，附带详尽的中文注释，让即便不精通 PyTorch 的读者也能一眼看懂算法逻辑。

---

## 二、 文章标准结构骨架（The 7-Step Article Skeleton）

全书每一篇长文必须完整包含以下 7 个结构段落，严禁缺漏：

| 步骤 | 模块 | 规范要求 |
| :--- | :--- | :--- |
| **01** | **Frontmatter** | 完整填写 `title`（7-12字短标题）、`description`、`series`、`chapter`、`order`、`type: concept`、`articleStatus: draft`、`prerequisites` |
| **02** | **一级标题与引言** | `# 标题`，开篇用生活场景/痛点切入，点明概念本质，配 **【全景流水线 / 架构图】**（`<figure>` + `<figcaption>`） |
| **03** | **痛点与因果演进** | 阐述传统方案的物理瓶颈，用直白因果解释新技术诞生的必然性 |
| **04** | **核心技术细节与代码** | 配合 **【微观机制图】** 与 **【极简可读代码卡片】** 展开底层运转逻辑，公式辅以大白话物理释义 |
| **05** | **现实权衡与奇特表象** | 剖析 1~2 个工业现实痛点（如「汉字刺客」、「算力快但显存装不下」、「一本正经胡说八道」等）及 **【工业权衡对比图】** |
| **06** | **`## 读到这里该能分清`** | 4~5 条精炼的核心认知要点总结（对比分明、适合日常交流引用） + 承上启下引出下一篇主题 |
| **07** | **`## 参考文献`** | 1~5 篇权威学术论文 / 官方技术报告，标准格式（作者、年份、斜体标题、arXiv/官网超链接） |

---

## 三、 版式排版与视觉设计（Typography & Visuals）

### 1. 字体与排版
- **中英文混排**：英文与中文之间保留自然空格，严格遵循 Kami 排版美学。
- **字体层级**：标题统一采用 `LXGW WenKai`（霞鹜文楷），正文采用 `Noto Sans SC`，代码与数值采用 `IBM Plex Mono`。

### 2. 数学公式规范
- **行内公式**：一律使用 `$...$` 语法（例如：`$O(L^2)$`）。
- **独立公式块**：一律使用 `$$...$$` 语法，并紧随提供大白话参数符号说明，确保 MathJax3 完美渲染。

### 3. 代码卡片规范
- 代码必须是真实、可运行的 Python 脚本；
- 保持短小精悍（15~30 行），注重表达核心算法心智，配有输入与 print 输出结果。

### 4. SVG 矢量图表与 Manim 动画规范
- **数量策略**：每篇文章根据复杂度配置 2~4 张图表/动画（默认 3 张：宏观全景图 $\to$ 微观机制图 $\to$ 工业权衡/桥梁图）。
- **配色标准**：继承 Kami 调色板（主色 `#1b365d`、强调色 `#2563eb`、底色 `#f8fafc` / `#ffffff`、边框 `#cbd5e1`）。
- **文字安全边界约束**：
  - 卡片宽度与文本长度必须精确计算；
  - 单行汉字/字符严禁超出容器宽度；
  - 容器内部必须保持 **20px+ 安全内边距**，杜绝任何贴边与视觉溢出。
- **视频与动画标准（Manim / WebM / MP4）**：
  - 核心物理动态过程推荐使用 Manim 渲染 60fps 高清矢量动画，统一压缩为 Web 友好格式（WebM / H.264 MP4）；
  - 网页嵌入标签一律使用 `<video autoplay loop muted playsinline poster="...">`。

### 5. 图表与媒体说明文字规范（Figcaption Standards）
- **极简短语化（Concise Labeling）**：`<figcaption>` 仅作为图表/视频的**主题标识与结构索引**，字数严格控制在 **10~25 字以内的短语或短句**（例如：`从 Logits 到概率分布的 Softmax 变换流程`、`全量重算 vs KV Cache 空间换时间机制对比`）。
- **严禁叙述冗余（Zero Narrative Redundancy）**：绝对禁止在 `<figcaption>` 中复制、复述正文大段原话、背景说明或因果推导。正文负责深度阐述，图题负责标识定位。
- **Alt 属性清晰**：图表 `<img>` 的 `alt` 属性应简明描述图表内容（供无障碍与屏幕阅读器使用），与 `<figcaption>` 互相配合但避免冗长复读。

### 6. 简介与摘要极简短语规范（Description Standards）
- **高信息密度短语化**：全书各专栏、章节与文章的 `description`、Frontmatter 简介以及卡片摘要必须采用 **【核心机制 + 关键技术/算子 + 物理结论】** 的极简短语，严格控制在 **15~28 字以内**。
- **严禁套话废话模板（Anti-patterns）**：
  - 严禁使用「从……讲到……」「在进入……前，先掌握……」等啰嗦流水账；
  - 严禁使用「带你看懂……究竟是怎么运转的」「以及为什么……」「拆解……」等口播推销式前缀；
  - 拒绝主谓宾完整的冗长复合长句，全面改用干净利落的专业概念短语组合。
- **对比示例**：
  - ❌ 错误（冗余）：`参数量与显存换算、显存带宽瓶颈，以及为什么每吐一个字都要搬运整套模型。`（34字）
  - ✅ 正确（精炼）：`参数显存占用换算、HBM 带宽瓶颈与单字生成延迟下限。`（18字）

---

## 四、 参考文献标准格式（Citations）

每篇文末必须列出真实的学术原著与官方技术报告，格式统一采用：
```markdown
## 参考文献

1. 作者名, 等. (年份). [*论文或报告标题斜体*](https://arxiv.org/abs/xxxx.xxxxx). 会议/期刊/arXiv 编号.
2. 机构名. (发布日期). [*报告或博客标题斜体*](https://official-link.com).
```

---

## 五、 交付与质检清单（Pre-flight QA Checklist）

在交付任何一篇文章前，必须依次完成：
- [ ] **易读性与生活直觉**：是否做到了直觉先行？是否消除了生硬公式与生僻算子的劝退感？
- [ ] **结构完整性**：是否包含 7 步骨架、`<figure>`、`## 读到这里该能分清` 与 `## 参考文献`。
- [ ] **视觉边界**：所有生成的 SVG 图表是否无文字溢出、无贴边。
- [ ] **图题精炼度**：所有 `<figcaption>` 是否控制在 10~25 字以内的短语标题，杜绝复述正文大段论述与冗长描述。
- [ ] **简介短语化**：所有 `description` 与 Frontmatter 简介是否控制在 15~28 字以内，杜绝「从……讲到……」「以及为什么……」等套话废话。
- [ ] **代码极简性**：代码是否短小精悍、注释通俗、带运行结果。
- [ ] **元数据同步**：`series.ts` 与对应 `.md` 的状态、标题是否同步。
- [ ] **自动化构建**：`npm run verify` 与 `npm run build` 全绿通过。
