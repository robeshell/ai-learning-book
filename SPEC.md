# 《看懂人工智能》概念长文写作与排版规范（SPEC）

本文档定义《看懂人工智能》全书（5 大主线专题 + 1 组基础知识 · 共 42 篇）技术长文的写作标准、架构深度要求、版式排版与工程质量交付流程。

---

## 一、 核心写作哲学（Philosophy & Standards）

本书定位为 **「严谨、透彻、接地气的 AI 工程师技术通识」**。读者包括转型 AI 的工程师、技术管理者及深度开发者。

文章写作必须遵循以下四大原则：

1. **直面本质，讲清数据流（No Fluff, Pure Mechanics）**：
   - 拒绝空洞的拟人化与玄学比喻，将大模型还原为浮点数权重、矩阵计算、非线性变换与概率分布；
   - 讲清楚输入张量是什么、经过了哪些矩阵运算、显存与算力开销是多少。
2. **时效性直达现代（2026 Reality）**：
   - 历史方案（如 RNN、经典 MHA、绝对正弦编码）只作为理解机制的推演阶梯；
   - 落脚点必须是现代主流开源与商业大模型（如 Llama 3、DeepSeek-V3、Claude、Qwen 2.5）真实采用的标准技术栈（RoPE, GQA/MLA, FlashAttention, RMSNorm, SwiGLU 等）。
3. **文码合一、真实验证（Trace the Tensors）**：
   - 代码是正文推演的直接印证，拒绝孤立脱节的代码块；
   - 代码中的变量名、数据样本与张量形状必须与正文完全一致；
   - 正文引用的张量数值必须在真实 Python/PyTorch 环境下跑通验证，确保真实准确。
4. **朴素克制，拒绝浮夸（Simple & Grounded Tone）**：
   - 语言平实通透，像工程师面对面交流；
   - **小标题必须简单淳朴、实事求是，严禁夸张煽情或辞藻堆砌**；
   - 严禁任何形式的自夸、吹捧与空洞宣称。

---

## 二、 文章自然流动结构（Article Flow）

拒绝死板教条的固定填空模板，按技术主题的逻辑自然展开四段式流动：

```
┌─────────────────────────────────────────────────────────────┐
│  1. 问题与本质（当前面临的数据/系统矛盾是什么？核心定义是什么？） │
├─────────────────────────────────────────────────────────────┤
│  2. 核心机制与图解（底层如何计算？张量如何流动？配机制图/动图） │
├─────────────────────────────────────────────────────────────┤
│  3. 现代工业演进（历史方案遇到什么瓶颈？现代架构如何解决？）    │
├─────────────────────────────────────────────────────────────┤
│  4. 最小代码与概念辨析（可运行 Python 代码 + 核心概念辨析）  │
└─────────────────────────────────────────────────────────────┘
```

| 模块 | 规范要求 |
| :--- | :--- |
| **Frontmatter** | 完整填写 `title`（7-14字短标题）、`description`（15-28字极简短语）、`series`、`chapter`、`order`、`type: concept`、`articleStatus: draft`、`prerequisites` |
| **引言与问题切入** | 抛出核心技术问题与数据状态，点明技术本质，保持平等专业的工程师对话视角 |
| **核心机制展开** | 步步推演，图文结合，小标题朴素平实，公式附带参数大白话释义 |
| **现代工业演进** | 阐明工程瓶颈（显存、带宽、长度外推）与 2024–2026 年现代主流落地实践 |
| **最小可用代码** | 纯 Python / PyTorch 可直接运行的代码，打印中间张量与输出 |
| **`## 核心概念辨析`** | 3~4 条精炼概念对比总结，平实客观，严禁居高临下或说教口吻 |
| **`## 参考文献`** | 权威论文与官方技术报告（包含 2023–2026 现代前沿文献），标准格式与链接 |

---

## 三、 版式排版与视觉设计（Typography & Visuals）

### 1. 字体与排版
- **中英文混排**：英文与中文之间保留自然空格。
- **字体层级**：标题采用 `LXGW WenKai`，正文采用 `Noto Sans SC`，代码与数值采用 `IBM Plex Mono`。

### 2. 数学公式规范
- 行内公式使用 `$...$`，独立公式块使用 `$$...$$`，确保 MathJax3 完美渲染。
- 公式后简要说明参数物理含义，避免堆砌无解释符号。

### 3. 代码规范
- 代码必须是真实、可运行的 Python/PyTorch 脚本，核心逻辑精炼（30~60 行）；
- 必须带控制台真实输出结果，数值位级对齐。

### 4. 图表与视频规范
- 动态过程配 Manim 渲染的 WebM / MP4 高清动画（`<video autoplay loop muted playsinline poster="...">`）；
- 静态拓扑配 SVG 矢量图，遵循 Kami 调色板（主色 `#1b365d`、强调色 `#2563eb`、底色 `#f8fafc` / `#ffffff`、边框 `#cbd5e1`）；
- **文字安全边界**：容器内部保留 **20px+ 安全内边距**，文字严禁溢出或贴边。

### 5. 图表说明文字规范（Figcaption Standards）
- **极简短语化**：`<figcaption>` 严格控制在 **10~25 字以内的短语标题**（如：`多头注意力在长句中的语法与属性解耦`）；
- **严禁叙述冗余**：严禁在 `<figcaption>` 中复述正文大段原话或长句。

### 6. 简介与导语极简短语规范（Description Standards）
- 全书各专栏、章节与文章的 `description` 严格控制在 **15~28 字专业概念短语**；
- 严禁使用「从……讲到……」「带你看懂……」「以及为什么……」等废话套话。

### 7. SVG 配图文字精炼与防溢出规范（SVG Diagram Standards）
- **副标题极简（10~22 字）**：`<text class="subtitle">` 必须为极简技术短语或公式，杜绝长段叙述；
- **卡片文本短语化（单行 ≤ 14 汉字）**：卡片内部严格采用紧凑技术术语或短语，详细阐述保留在 Markdown 正文中；
- **容器与文本安全余量（Padding ≥ 16px）**：卡片边界与文字留足空隙，严禁贴边、溢出或与线条重叠。

---

## 四、 质量验收自检流程

所有文章提交前必须通过双重自动化校验：
1. `npm run verify`：检查 Frontmatter、字数、标题规范、图注长度、必要章节完整性；
2. `npm run build`：检查 VitePress 构建无死链、无语法错误。
